export class User {
    constructor(rut, name, email, password, birthdate) {
        this.rut = rut
        this.name = name
        this.email = email
        this.password = password
        this.birthdate = birthdate
    }
}
