// Componente MapView para web (placeholder)
import { View, Text } from 'react-native'

// Componentes dummy para web
export const MapView = ({ children, ...props }) => (
    <View {...props}>
        {children}
    </View>
)

export const Marker = () => null
